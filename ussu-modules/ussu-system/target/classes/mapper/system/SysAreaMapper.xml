<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ussu.modules.system.mapper.SysAreaMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.ussu.modules.system.entity.SysArea">
        <id column="id" property="id" />
        <result column="code" property="code" />
        <result column="name" property="name" />
        <result column="parent_id" property="parentId" />
        <result column="simple_name" property="simpleName" />
        <result column="level" property="level" />
        <result column="citycode" property="citycode" />
        <result column="yzcode" property="yzcode" />
        <result column="mername" property="mername" />
        <result column="lng" property="lng" />
        <result column="lat" property="lat" />
        <result column="pinyin" property="pinyin" />
        <result column="del_flag" property="delFlag" />
    </resultMap>
    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, code, name, parent_id, simple_name, level, citycode, yzcode, mername, lng, lat, pinyin, del_flag
    </sql>

    <select id="findCascaderByParendId" resultType="java.util.Map">
        SELECT a.id AS value,a.name AS label,(SELECT count(1) FROM sys_area WHERE parent_id = a.id) AS haveChildren,
        a.citycode,a.yzcode,a.mername,a.lng,a.lat,a.pinyin FROM sys_area a
        WHERE a.del_flag =0 AND a.parent_id = #{pid} ORDER BY a.pinyin ASC
    </select>

</mapper>
